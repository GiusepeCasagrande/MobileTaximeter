<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
		xmlns:maps="clr-namespace:Xamarin.Forms.Maps;assembly=Xamarin.Forms.Maps" 
		xmlns:exmaps="clr-namespace:Taxi.Controls;assembly=Taxi" 
		x:Class="Taxi.Pages.MapPage">
	<StackLayout VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand" Padding="0" Spacing="0">
		<exmaps:ExtendedMap
				IsShowingUser="true"
				WidthRequest = "400"
				HeightRequest = "400"
				VerticalOptions="FillAndExpand" 
				HorizontalOptions="FillAndExpand" 
				x:Name="MainMap"
				MapType="Street" 
				CenterPoint="{Binding CenterPoint}"
				Radius="{Binding Radius}" />
		<Button Text="{Binding ButtonText}" Command="{Binding StartRunCommand}">
		</Button>
	</StackLayout>
</ContentPage>